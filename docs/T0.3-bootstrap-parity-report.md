# T0.3 — bootstrap reproducible y cierre de ACL

Fecha: 2026-09-04. Rama: `codex/vercel-native-runtime`.
Base del avance: `5d0bf20c629a9ed14b1cea13d9d29438b7167010`.

**BRANCHING READY — GO PARA CREAR STAGING, bajo una autorización separada.**

El último bloqueo quedó cerrado con el REVOKE UPDATE autorizado sobre cinco
secuencias. No se creó staging ni se ejecutó ninguna modificación productiva.
Este informe sustituye los cierres anteriores NOT READY; conserva la explicación
y evidencia de las correcciones previas en el manifest del bootstrap.

## Resultado de los 32 puntos solicitados

| # | Verificación | Resultado |
| --- | --- | --- |
| 1 | Cinco secuencias | Nombres exactos en la tabla ACL siguiente |
| 2 | ACL antes | UPDATE para service_role en las cinco; owner postgres; ningún otro desajuste inesperado en 244 comprobaciones |
| 3 | ACL después | Coinciden con producción; tabla exacta siguiente |
| 4 | Permiso revocado | Sólo UPDATE de service_role, cinco true→false |
| 5 | Permisos preservados | Ningún USAGE/SELECT revocado; owners, otros roles y otras secuencias intactos |
| 6 | Paridad local/productiva | 244/244 privilegios efectivos de funciones/secuencias coinciden; diferencias de plataforma y contrato explícitas debajo |
| 7 | Replay | PASS, reset local vacío → L1…L12 → T0.3, 13 migraciones |
| 8 | reservation_code | PASS, cuatro reservas con códigos RES- de ocho dígitos, únicos; creación RPC y walk-in |
| 9 | reservation history | PASS, ID positivo generado en transición a checked_out |
| 10 | room status history | PASS, ID positivo en cambio real pending_cleaning→cleaning mediante RPC |
| 11 | activity logs | PASS, ID positivo y evento room.status_changed; también payment.voided |
| 12 | audit logs | PASS, ID positivo y snapshot del cambio de habitación; triggers sensibles operativos |
| 13 | Auth | PASS local real: password login, active/owner con JWT/RLS, refresh, logout y rechazo del refresh revocado |
| 14 | RLS/RBAC | PASS: owner, anon, sin rol, pending, disabled, roles aislados y helpers intencionales |
| 15 | Pagos | PASS: pago válido, errores, saldo 150→140, anulación de pago 50→saldo 190, ledger preservado y doble anulación rechazada |
| 16 | Redacción | PASS: siete tablas, INSERT y UPDATE previstos; ausencia de claves sensibles en snapshots y de documento en toda auditoría |
| 17 | Anti-overbooking | PASS con dos conexiones: un commit y un rechazo 23P01 |
| 18 | Wellness concurrency | PASS último cupo: una reserva/pago y un rechazo WELLNESS_CAPACITY_EXCEEDED |
| 19 | Datos finales | Todos los conteos de negocio, Auth y Storage de prueba = 0; sólo seeds estructurales |
| 20 | Tests aplicación | 191/191 PASS; regresiones SQL/HTTP y concurrencia adicionales PASS |
| 21 | Lint | PASS |
| 22 | TypeScript | PASS |
| 23 | Build | PASS; también build interno de npm test |
| 24 | npm audit | 0 vulnerabilidades; npm ci PASS, 366 paquetes |
| 25 | git diff --check | PASS; nuevos archivos revisados por whitespace y credenciales |
| 26 | Commit | Local: chore: make database bootstrap reproducible; SHA exacto en la entrega final y recuperable con el comando debajo |
| 27 | Working tree | Sólo 15 archivos del avance T0.3; limpieza después del commit verificada en la entrega final |
| 28 | NO PROD CHANGES | Sólo SELECT remotos; historial 52, última 20260903035847, T0.3 aplicada=0 |
| 29 | NO PUSH | Sin push, PR ni merge |
| 30 | NO DEPLOY | Sin deploy, sin staging, sin cambios Vercel |
| 31 | Readiness | BRANCHING READY |
| 32 | Decisión | GO PARA CREAR STAGING; no equivale a crearlo en esta tarea |

El SHA no se incrusta dentro del mismo documento que genera ese SHA. Para
identificar el commit de este informe, además de la entrega final:

```powershell
git log -1 --format=%H -- docs/T0.3-bootstrap-parity-report.md
```

## Auditoría previa y ACL exactas

`r`=SELECT, `w`=UPDATE, `U`=USAGE. En todos los casos el owner es postgres.
Su identidad es distinta del usuario Auth que posee el rol de aplicación owner.

| Secuencia | ACL local previa | ACL local final = ACL productiva READ ONLY |
| --- | --- | --- |
| public.activity_logs_id_seq | {postgres=rwU/postgres,service_role=w/postgres} | {postgres=rwU/postgres} |
| public.audit_logs_id_seq | {postgres=rwU/postgres,service_role=w/postgres} | {postgres=rwU/postgres} |
| public.reservation_code_seq | {postgres=rwU/postgres,service_role=w/postgres,authenticated=rU/postgres} | {postgres=rwU/postgres,authenticated=rU/postgres} |
| public.reservation_status_history_id_seq | {postgres=rwU/postgres,service_role=w/postgres} | {postgres=rwU/postgres} |
| public.room_status_history_id_seq | {postgres=rwU/postgres,service_role=w/postgres} | {postgres=rwU/postgres} |

En las cinco, service_role pasa de U/r/w = no/no/sí a no/no/no. Ningún permiso
de ese rol distinto de UPDATE se revocó. Los permisos fuera de estas cinco
secuencias permanecen iguales. No se utilizó ninguna clave de service_role.

anon: no/no/no antes y después. authenticated: no/no/no en las cuatro identities;
sólo reservation_code_seq conserva sí/sí/no. postgres conserva sí/sí/sí.

La consulta previa incluyó raw ACL, privilegios efectivos por rol, owner,
dependencias de identity/default y funciones que insertan en las tablas.
La comparación completa de funciones/secuencias mostraba exactamente cinco
diferencias UPDATE antes del cambio y ninguna después.

### Consumidores legítimos comprobados

| Secuencia | Uso | Contexto real |
| --- | --- | --- |
| activity_logs_id_seq | Identity activity_logs.id | private.log_activity, llamado por RPC operativas |
| audit_logs_id_seq | Identity audit_logs.id | private.log_audit, capture_sensitive_change, capture_media_asset_change |
| reservation_code_seq | Default reservations.code = RES- + nextval | create_reservation, create_reservation_v2, create_walk_in |
| reservation_status_history_id_seq | Identity reservation_status_history.id | create_reservation/v2, create_walk_in, cancel_reservation, perform_check_in/out |
| room_status_history_id_seq | Identity room_status_history.id | create_walk_in, perform_check_in/out, set_room_operational_status |

Los consumidores privilegiados examinados son SECURITY DEFINER, owner postgres.
No necesitan ejecutar las secuencias con identidad service_role. No se alteró
ninguna definición, owner, RLS ni policy en esta ampliación.

## SQL de esta ampliación, y nada más

Archivo: `supabase/migrations/20260904001947_bootstrap_parity_and_security.sql`.

```sql
revoke update on sequence
  public.activity_logs_id_seq,
  public.audit_logs_id_seq,
  public.reservation_code_seq,
  public.reservation_status_history_id_seq,
  public.room_status_history_id_seq
from service_role;
```

Se verificó que, quitando exactamente este bloque y sus comentarios, T0.3 queda
idéntica a su contenido al comenzar esta autorización. L1–L12 no se modificaron.
No se usó REVOKE ALL, CASCADE, ALTER DEFAULT PRIVILEGES ni grants adicionales.

La guía Supabase motivó la comprobación de permisos efectivos y pruebas reales;
la sintaxis mínima se contrastó con
[REVOKE de PostgreSQL 17](https://www.postgresql.org/docs/17/sql-revoke.html).
No se generó otra migración ni se volvió a crear la base productiva.

## Permisos anteriores que permanecen corregidos

T0.3 conserva el endurecimiento autorizado en la fase anterior:

- Sin EXECUTE de usuarios finales sobre enforce_rate_limit, hostel_today,
  log_activity, log_audit, reservation_balance y reservation_paid_total.
- authenticated conserva EXECUTE sobre has_permission e is_active_staff, usado
  por RLS. require_permission no es invocable directamente, pero funciona
  dentro de las RPC SECURITY DEFINER.
- Las dos secuencias wellness mantienen su ACL ya corregida, sin cambios ahora.
- La función histórica validate_room_inventory_assignment retorna trigger y
  conserva su ACL, igual a producción; no es una RPC invocable directamente.

Se verificaron todas las dependencias de funciones en las policies examinadas,
lecturas owner de todas las tablas públicas y pruebas RBAC de permisos aislados.
Los tests negativos ejecutan realmente setval, nextval y SELECT como service_role
sobre las cinco secuencias y comprueban 42501; también prueban nextval/SELECT
legítimos como authenticated sobre reservation_code_seq.

## Paridad, defaults y diferencias clasificadas

El snapshot abarca 40 funciones de aplicación y siete secuencias con PUBLIC,
anon, authenticated y service_role: **244 comprobaciones, 27 entradas permitidas**.
Esta autorización produce exactamente cinco revocaciones y cero ampliaciones.
Los demás 239 resultados permanecen idénticos. Las ACL finales coinciden con
producción READ ONLY en los 244 resultados comunes.

El catálogo de objetos/definiciones antes y después permanece idéntico:
42 relaciones, 336 columnas, 199 constraints, 91 índices, 16 enums, 49 triggers,
60 policies, RBAC 5/26/65 y seis servicios. También permanecen iguales los 490
checks de grants de relaciones, 2688 de columnas, 8 de schemas y 42 de secuencias
anon/authenticated. Cuerpos de funciones y las dos secuencias wellness intactos
durante esta ampliación.

| Diferencia restante | Clasificación | Tratamiento |
| --- | --- | --- |
| public.rls_auto_enable sólo en Supabase hospedado | EXPECTED | Función de plataforma, cuatro entradas ACL adicionales remotas; no copiar internals |
| Representación NULL/default del owner en secuencias wellness remotas | EXPECTED | Derechos efectivos equivalentes, no acceso adicional de roles API |
| Default privileges de nuevas secuencias public | EXPECTED de plataforma, riesgo futuro conocido | Los objetos actuales tienen ACL explícitas correctas; readiness rechaza cualquier nueva secuencia con UPDATE indebido |
| register_payment: contrato SQLSTATE explícito local frente al contrato remoto antiguo | INTENTIONAL HARDENING, heredado de la autorización T0.3 | Ninguna modificación nueva de la función; futura decisión productiva separada |
| DRIFT de seguridad de objetos de aplicación | Ninguno detectado | Gate readiness PASS |

No se alteran defaults globales de postgres: tienen alcance fuera de estos
objetos y dependen del rol creador. Los defaults de plataforma no se aceptan como
ACL final de las secuencias. Las migraciones futuras deben fijar ACL explícitas
y pasar el contrato/regresión; el gate está integrado en test y replay.
Esto cierra el drift de objetos sin introducir cambios globales no autorizados.

## Replay y datos de prueba

Destino exclusivo: proyecto Docker `casa-albor-bootstrap`, contenedor
`supabase_db_casa-albor-bootstrap`, API local 55421 y PostgreSQL local 55422.
CLI 2.116.0; PostgreSQL 17.6; Docker Engine 29.6.2. No link a
jduitbuzomkwmzzyrjux ni marcadores project-ref/pooler-url. El harness rechaza
selectores de base remota, flags extra y Docker remoto; usa socket local dentro
del contenedor y no carga .env.local. No lee credenciales productivas.

El reset recreó la base local antes del replay. Los tests transaccionales hacen
ROLLBACK; los concurrentes y Auth requieren fixtures locales confirmados y un
reset obligatorio en finally. Al finalizar volvió a ejecutarse L1→L12→T0.3 y el
gate confirmó estructura, datos vacíos y ACL.

Conteos finales locales, todos en 0:
guests, reservations, payments, rooms, beds, wellness_products, wellness_slots,
wellness_bookings, Auth users/identities/sessions/refresh_tokens y Storage objects.
El control adicional revisa todas las tablas públicas no estructurales y
private.operation_rate_limits.

Sólo quedan roles=5, permissions=26, role_permissions=65, room_services=6,
bucket hostel-media y cuatro audit_logs estructurales de asociaciones owner
media/wellness. No existen fixtures en migraciones/seed.sql ni fotos cargadas.

Las pruebas Auth usan contraseña sintética efímera generada dentro de PostgreSQL
local. Claves/tokens quedan en memoria y nunca se muestran ni guardan. No se
probó login de una persona real contra producción.

## Pagos, auditoría y concurrencia

Pago 50 produce saldo 150; otro pago autorizado de 10 produce 140.
Anular el pago de 50 produce saldo 190, sin eliminar el ledger. Un segundo void
del mismo pago devuelve 23505 PAYMENT_ALREADY_VOIDED. Permanecen las pruebas de
monto inválido/no positivo, reserva inexistente, exceso y falta de permiso.

Se verifican INSERT en las siete tablas de redacción y UPDATE de
guests/profiles/internal_notes, además del snapshot generado al anular el pago.
Los snapshots no contienen las claves sensibles previstas. Los eventos mínimos
de negocio pueden incluir importes legítimos: no se afirma redacción total de
todos los datos financieros de auditoría.

La prueba de habitaciones utiliza dos conexiones simultáneas y la exclusión
room_assignments_no_overlap: un commit, un rechazo 23P01.
La de wellness consume el último cupo mediante RPC: un commit y un rechazo
23P01 WELLNESS_CAPACITY_EXCEEDED. Producto/slot se ejercitan mediante harness,
incluyendo save_wellness_product/save_wellness_slot y creación de booking/pago.

## Advisors, validaciones y límites

Advisors locales: **0 avisos de seguridad**, diez WARN de rendimiento
multiple_permissive_policies preexistentes, sin nuevas policies en T0.3.
No se corrigieron fuera de alcance. El advisor complementa, no sustituye, las
comparaciones de ACL y las pruebas.

Comandos completados:

```powershell
npm ci
node scripts/database-local.mjs replay
node scripts/database-local.mjs readiness
node scripts/database-local.mjs advisors
npm run lint
npm run typecheck
npm run build
npm test
npm audit
git diff --check
git check-ignore -v .env.local
```

También: SELECT de catálogo local/productivo, revisión de diffs, dependencias
y secrets. La primera lectura de ayuda del CLI encontró restricción local de
telemetría; se repitió con telemetría desactivada y terminó correctamente.
No hubo errores en la migración ni regresiones funcionales pendientes.

## Archivos del commit T0.3

Quince archivos del avance acumulado autorizado:

- package.json (sólo scripts; ninguna dependencia/version alterada).
- supabase/.gitignore y supabase/config.toml.
- supabase/migrations/20260904001947_bootstrap_parity_and_security.sql.
- scripts/database-local.mjs.
- tests/database-local-safety.test.mjs.
- tests/database/acl-contract.json, acl-readiness.sql, acl-snapshot.sql.
- tests/database/bootstrap-schema.sql, catalog.sql, critical-tests.mjs, fixtures.sql.
- docs/database-migration-baseline.md y este informe.

L1–L12, package-lock.json y AGENTS.md intactos. .env.local continúa ignorado y
no versionado. No hay secretos, logs operativos ni datos reales en el commit.
El stack local se detiene al concluir, conservando el respaldo local de volúmenes.

Producción: historial=52, última=20260903035847, T0.3 aplicada=0. Los conteos
remotos consultados de huéspedes, reservas, pagos, productos/slots/bookings
wellness siguen en 0. Sólo se hicieron lecturas.

No migration repair, db push remoto, push Git, PR, merge, despliegue ni staging.
El siguiente paso es obtener autorización y destino de staging separados; no
aplicar esta historia canónica directamente contra producción.
